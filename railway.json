{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"cd packages/database && npx prisma migrate deploy && npx prisma db seed && cd /app && node apps/api/dist/index.js\"",
    "healthcheckPath": "/health"
  }
}
